$(function ()
  {
    $(".window").each(function ()
  {
    var myDiv = $(this);
    myDiv.resizable();
  });

    // // Dynamically load pages
    // $('#portfolio-info-pane').load("html/thelastofus2_small.html");
  });
